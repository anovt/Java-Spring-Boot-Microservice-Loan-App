server:
  port: 8072

eureka:
  instance:
    preferIpAddress: true
  client:
    resgister-with-eureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: "http://localhost:8070/eureka/"